<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lyric.lyric.Mapper.content.DiaryMapper">
    
    <!-- 结果映射 -->
    <resultMap id="diaryResultMap" type="com.lyric.lyric.Pojo.content.DiaryPojo">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="summary" column="summary"/>
        <result property="contentType" column="content_type"/>
        <result property="contentFormat" column="content_format"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="isDraft" column="is_draft"/>
        <result property="emotionScore" column="emotion_score"/>
        <result property="wordCount" column="word_count"/>
        <result property="writingStartTime" column="writing_start_time"/>
        <result property="writingEndTime" column="writing_end_time"/>
        <result property="writingDuration" column="writing_duration"/>
        <result property="diaryDate" column="diary_date"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <!-- 插入日记 -->
    <insert id="insert" parameterType="com.lyric.lyric.Pojo.content.DiaryPojo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO diary(title, content,  summary, content_type, content_format, is_deleted, is_draft, emotion_score, word_count, writing_start_time, writing_end_time, writing_duration, diary_date)
        VALUES(#{title}, #{content}, #{summary}, #{contentType}, #{contentFormat}, #{isDeleted}, #{isDraft}, #{emotionScore}, #{wordCount}, #{writingStartTime}, #{writingEndTime}, #{writingDuration}, #{diaryDate})
    </insert>
    
    <!-- 根据ID查询日记 -->
    <select id="selectById" parameterType="long" resultMap="diaryResultMap">
        SELECT * FROM diary WHERE id = #{id}
    </select>
    
    <!-- 查询所有日记 -->
    <select id="selectAll" resultMap="diaryResultMap">
        SELECT * FROM diary
    </select>
    
    <!-- 更新日记 -->
    <update id="update" parameterType="com.lyric.lyric.Pojo.content.DiaryPojo">
        UPDATE diary
        SET title=#{title}, content=#{content}, summary=#{summary}, content_type=#{contentType}, content_format=#{contentFormat},
            is_deleted=#{isDeleted}, is_draft=#{isDraft}, emotion_score=#{emotionScore}, word_count=#{wordCount},
            writing_start_time=#{writingStartTime}, writing_end_time=#{writingEndTime}, writing_duration=#{writingDuration},
            diary_date=#{diaryDate}, updated_at=CURRENT_TIMESTAMP
        WHERE id=#{id}
    </update>
    
    <!-- 根据ID删除日记 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM diary WHERE id = #{id}
    </delete>
    
</mapper>