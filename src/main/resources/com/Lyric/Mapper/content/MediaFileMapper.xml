<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lyric.lyric.Mapper.content.MediaFileMapper">
    
    <!-- 结果映射 -->
    <resultMap id="mediaFileResultMap" type="com.lyric.lyric.Pojo.content.MediaFilePojo">
        <id property="id" column="id"/>
        <result property="diaryId" column="diary_id"/>
        <result property="fileName" column="file_name"/>
        <result property="filePath" column="file_path"/>
        <result property="fileType" column="file_type"/>
        <result property="fileSize" column="file_size"/>
        <result property="uploadTime" column="upload_time"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>
    
    <!-- 插入多媒体文件 -->
    <insert id="insert" parameterType="com.lyric.lyric.Pojo.content.MediaFilePojo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO media_file(diary_id, file_name, file_path, file_type, file_size, upload_time)
        VALUES(#{diaryId}, #{fileName}, #{filePath}, #{fileType}, #{fileSize}, #{uploadTime})
    </insert>
    
    <!-- 根据ID查询多媒体文件 -->
    <select id="selectById" parameterType="long" resultMap="mediaFileResultMap">
        SELECT * FROM media_file WHERE id = #{id}
    </select>
    
    <!-- 根据日记ID查询多媒体文件列表 -->
    <select id="selectByDiaryId" parameterType="long" resultMap="mediaFileResultMap">
        SELECT * FROM media_file WHERE diary_id = #{diaryId}
    </select>
    
    <!-- 查询所有多媒体文件 -->
    <select id="selectAll" resultMap="mediaFileResultMap">
        SELECT * FROM media_file
    </select>
    
    <!-- 更新多媒体文件 -->
    <update id="update" parameterType="com.lyric.lyric.Pojo.content.MediaFilePojo">
        UPDATE media_file
        SET diary_id=#{diaryId}, file_name=#{fileName}, file_path=#{filePath},
            file_type=#{fileType}, file_size=#{fileSize}, upload_time=#{uploadTime}
        WHERE id=#{id}
    </update>
    
    <!-- 根据ID删除多媒体文件 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM media_file WHERE id = #{id}
    </delete>
    
</mapper>